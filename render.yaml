services:
  - type: web
    name: ft-osint-bot-hosting
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements_hosting.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
